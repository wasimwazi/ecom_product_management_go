package category

const (
	//ListCategoryLimit default limit for category listing
	ListCategoryLimit = 10
	//Offset default offset value for category listing
	Offset = 0
	//DefaultCategory default value when no category is specified
	DefaultCategory = 0
)
